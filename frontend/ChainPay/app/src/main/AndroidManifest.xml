<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zeyuzhang.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.FunctionActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.CheckActivity" />
        <activity android:name=".activity.FaceActivity" />
        <activity android:name=".activity.TransferActivity" />
        <activity android:name=".activity.FaceTransferActivity" />
        <activity android:name=".activity.ReceiveActivity" />
        <activity android:name=".activity.OkToTradeActivity" />
        <activity android:name=".activity.ReadyToInsertFace" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TransferInquireActivity" />
        <activity android:name=".activity.MineActivity" />

        <service android:name="com.vaskka.frontend.service.PollingService" />

        <activity android:name=".activity.ChargeActivity"></activity>
    </application>

</manifest>